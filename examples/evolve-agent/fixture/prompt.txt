The workspace already contains a small broken Python package:

- stats.py — buggy mean / percentile
- test_stats.py — failing tests

Fix stats.py so every test passes. Do not rewrite the tests unless a test
is clearly wrong. Verify with:

  python3 -c "from test_stats import test_mean, test_mean_empty, test_percentile; test_mean(); test_mean_empty(); test_percentile(); print('ok')"

Prefer reading existing files before editing. ok=true only when verify exits 0.
