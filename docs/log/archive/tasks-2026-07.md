# Task archive — 2026-07

Completed task batches moved here from `TASKS.md`.

## 2026-07-15 — Milestones M0–M9

- [x] Bootstrap: Cabal package, docs pack, Cursor scaffold
- [x] Milestones M0–M8 (see [spec/10-acceptance.md](../../spec/10-acceptance.md))
- [x] Initial multi-file language/runtime specification
- [x] M0 syntax skeleton: parser + AST + pretty + markdown loader
- [x] M1 pure evaluator: values, prelude builtins, module funs, E01/E02
- [x] M2 type checker: I/O vs main, local inference, schema(T)
- [x] M3 effects lattice + `hwfl check` single-module CLI
- [x] M4 host runtime + LlmProvider + snapshots + `hwfl run`
- [x] M5 `par` + `confirm` + step/resume/approve + machine_json
- [x] M6 spans.jsonl + `hwfl show` + redaction
- [x] M7 `llm.agent` loop + `tool(f)` typed tools
- [x] M8 slim semantic-check dogfood + LOC/file delta vs hwfi
- [x] M8 follow-up: valid JSON semantic report in run folder; `json.encode`; `ctx`
- [x] Full kont / `machine_json` codec (M5)
- [x] M9 project-wide `hwfl check` (`project.json` + import graph)
- [x] Float / `==` polymorphism cleanup
- [x] `llm.object` + schema reflection at runtime (E14)
- [x] `llm.agent_object` + synthetic `submit` tool
- [x] Polymorphic `obs.span` check type (E16)

## 2026-07-16 — Schema / agent UX polish

- [x] Add parameter descriptions to builtin agent tools
- [x] Allow optional `## schema Typename` sections in module markdown for
      `schema(T)` field descriptions
- [x] CLI `.env` load at startup

## 2026-07-16 — P0 coding-agent host gaps

- [x] Runtime `exec.run` + allowlist / `exec.confirm` from `project.json`
- [x] FS host ops: `fs.list`, `fs.edit`, `fs.grep` (agent-tool eligible)
- [x] Language reference card ([language-reference.md](../../language-reference.md))

## 2026-07-17 — Coding-agent example

- [x] Real coding-agent example (`examples/coding-agent`) — universal
      create/fix loop via `llm.agent_object` + FS + `exec.run`
- [x] `fs.find` agent-tool eligible
- [x] Mock fixture `CodingAgentSpec` (empty workspace → Python files + verify)

## 2026-07-17 — Detailed traces

- [x] Per-tool-call spans (`tool:<name>`, `SkAgentTool`) + nested host effects
- [x] `hwfl show` prints compact span attrs
- [x] CLI `hwfl run --debug` (live open/close + end tree)

## 2026-07-17 — Lifecycle tutorial

- [x] Author-facing tutorial: module → check → run → approve/resume → show
      ([tutorial.md](../../tutorial.md))

## 2026-07-17 — Skills phases A–C

- [x] Catalog + check (`skills/`, `skill:` frontmatter, instruction vs callable)
- [x] Host ops `skill.discover` / `skill.load` + project skill budgets
- [x] Agent mid-loop load + snapshot ids + rebuild-from-ids instructions
- [x] Example `examples/skills`; tests SkillCatalogSpec + SkillSpec

## 2026-07-17 — Coding-agent stack skills

- [x] Instruction skills for python / react / haskell / rust under
      `examples/coding-agent/skills/`
- [x] Wire `skill.discover` / `skill.load` into coding-agent toolbox
- [x] Mock fixture discovers/loads python skill before write/verify

## 2026-07-17 — Streaming LLM spans

- [x] `StreamDelta` + `ChatRequest.chatOnChunk` (obs side channel)
- [x] Coalescing sink → `events.jsonl` `llm.delta` + `--debug` echo
- [x] Mock fake chunks; llm-simple `streamTextWithFallbacks`
- [x] Wire `llm.chat` + `agent_round`; ProviderSpec + StreamSpec

## 2026-07-17 — `--json` CLI errors

- [x] Thin error envelopes on stderr for `check` / `run --json`
- [x] Success output unchanged; `--output json` remains separate

## 2026-07-17 — Semantic-check deepen

- [x] Body-bearing `review_gate` (redundancy / contradiction / speech /
      dead-ref; max 8; entropy outliers excluded)
- [x] Same-run optional layer 3 (`mode=pragmatic` + `llm.object`)
- [x] Runtime `loadTypeEnv` elaborates `main` I/O for `schema(T)` aliases
- [x] SemanticCheckSpec deterministic + pragmatic; 140 tests
- [x] Prose noise fix: module-tree scan + `text.is_qname` hygiene;
      coding-agent dogfood cleaned up

## 2026-07-17 — Semantic-check A+B (contradiction + redundancy)

- [x] Policy gate `check_internal_conflict` on skills / system / rules
- [x] Within-slice quoted redundancy (similarity > 0.9); H1-only synthetic
      skill slices; gate cap 8

## 2026-07-17 — `fs.patch` (Tier A)

- [x] Unique multi-hunk atomic edit; soft `{ ok, applied, error }`
- [x] Agent-tool eligible; coding-agent prefers over replace-all `fs.edit`

## 2026-07-17 — Semantic-check S2 (obligation graph)

- [x] `PragmaticOut.obligations` extract on gated reviews
- [x] Deterministic graph: must∧must_not / soft system-vs-skill /
      catalog-missing object (quoted evidence)
- [x] Fixtures require-search / forbid-search / ghost-tool + Spec
- [x] Crunch-budget fix: ≤4 obs/slice, ≤12 graph rows, finding caps 4;
      skip soft pass when n>8; pure crunch 500k

## 2026-07-17 — Semantic-check S1 (role typing)

- [x] `PragmaticOut.role` + `mismatched_sentences` on gated reviews
- [x] Deterministic Policy/System + Example felicity (`category: role`)
- [x] Fixture example-hard-rule + Spec

## 2026-07-17 — Semantic-check S5 (prose↔code contracts)

- [x] Dead bindable `@section` / effect+tool gaps / schema vs outputs /
      skill exec vs caller (`category: contract`)
- [x] Fixtures exec-gap / output-gap / skill-exec-gap + recommend-exec
- [x] Spec + coding-agent dogfood contract-clean

## 2026-07-17 — Semantic-check S3 (proposition algebra)

- [x] `PragmaticOut.propositions` + obligation projection / merge
- [x] Must∧MustNot + Must vs Prefer(~a) (`category: proposition`)
- [x] Fixture prefer-no-search; gate cap 10; Spec

## 2026-07-17 — `hwfl run --cost`

- [x] Prefix host progress lines with running LLM spend
- [x] Shared counter with `--debug` span cost; flag gates prefix only

## 2026-07-19 — Frontmatter `examples`

- [x] Optional `examples:` parse into `fmExamples` / `ExampleInputs`
- [x] `hwfl check` key-set match + duplicate names; values untyped
- [x] Spec §01 §2.1; dogfood `examples/summarise.md`; tests

## 2026-07-19 — CLI `--interactive`

- [x] `hwfl run --interactive` stdin loop for confirm / choice / ask
- [x] Same resolve path as `approve` / `choose` / `reply`; refuse
      `--json` / non-TTY

## 2026-07-20 — Lab spine + chat + E11 (archived from TASKS)

- [x] Library driver façade — check / run / step / resume / approve / show
- [x] Run-store interface over `.hwfl/runs` (FS backend)
- [x] `meta.invoke` / `meta.list_runs` / `meta.read_spans` /
      `meta.read_snapshot` (redact)
- [x] Local compare / genetic prototype — `examples/compare`
- [x] Observer hook (`stderrDebugObserver`; WS/SSE maps onto same)
- [x] Coding-agent chat — `Turn`; `llm.agent` history; example
- [x] Resume/approve recomputes `projectHashForModules` (hwfl-server)
- [x] FS tree ops — `mkdir` / `copy` / `move` / `exists` / `stat`
- [x] E11 same-project entry call (`FrInvoke`) —
      `examples/call-inner-workflow`

## 2026-07-20 — Compare mutate / next-gen loop

- [x] Mutate / next-generation loop on the compare spine (score →
      change genomes → re-run) — in-language only; no new host ops

## 2026-07-20 — Evolve-agent lab (E23)

- [x] Non-toy evolve loop: coding-agent genomes + LLM mutate + fallback
- [x] Richer lab fitness — task outcome `ok` + llm span count (E23)

## 2026-07-20 — Evolve-agent operators + fixture

- [x] Harder seeded fixture + varied mutation operators + no-op rejection

## 2026-07-21 — Runtime integrity High #1

- [x] Nested snapshot persist — `rcNestDepth` / outer-only writes for
      agent tools, `FrInvoke`, and `par` (`NestedSnapshotSpec`)

## 2026-07-21 — Runtime integrity High #2

- [x] `meta.invoke` sandbox — `project` / `workspace` through
      `resolvePath` + canonicalize (`mkdirPath` for workspace);
      abs / `../` / symlink parent rejected (`MetaInvokeSpec`)

## 2026-07-21 — Runtime integrity High #3

- [x] Crash-safe store + run IDs — atomic `meta.json` / `snapshot.json`
      (temp + rename); collision-resistant `newRunId` (`StoreSpec`);
      multi-process locking deferred

## 2026-07-22 — Credible skill-driven coding-agent

- [x] Chat layer — `human.ask`; tools `coding_session` + `gather_context`
      (no `fs.write` on chat)
- [x] Coding session FrInvoke — `workflows/coding` typed plan → serial
      do_task / verify (one retry then stop)
- [x] `workflows/gather_context` — read-only token-budget pre-pass
- [x] `workflows/verify` — `exec.run` → `{ exit, stdout, stderr, ok }`
- [x] FrInvoke callee fun-table fix; `CodingAgentSpec` +
      `SimpleCodingAgentSpec`
