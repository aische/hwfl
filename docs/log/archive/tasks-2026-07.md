# Task archive — 2026-07

Completed task batches moved here from `TASKS.md`.

## 2026-07-15 — Milestones M0–M9

- [x] Bootstrap: Cabal package, docs pack, Cursor scaffold
- [x] Milestones M0–M8 (see [spec/10-acceptance.md](../../spec/10-acceptance.md))
- [x] Initial multi-file language/runtime specification
- [x] M0 syntax skeleton: parser + AST + pretty + markdown loader
- [x] M1 pure evaluator: values, prelude builtins, module funs, E01/E02
- [x] M2 type checker: I/O vs main, local inference, schema(T)
- [x] M3 effects lattice + `hwfl check` single-module CLI
- [x] M4 host runtime + LlmProvider + snapshots + `hwfl run`
- [x] M5 `par` + `confirm` + step/resume/approve + machine_json
- [x] M6 spans.jsonl + `hwfl show` + redaction
- [x] M7 `llm.agent` loop + `tool(f)` typed tools
- [x] M8 slim semantic-check dogfood + LOC/file delta vs hwfi
- [x] M8 follow-up: valid JSON semantic report in run folder; `json.encode`; `ctx`
- [x] Full kont / `machine_json` codec (M5)
- [x] M9 project-wide `hwfl check` (`project.json` + import graph)
- [x] Float / `==` polymorphism cleanup
- [x] `llm.object` + schema reflection at runtime (E14)
- [x] `llm.agent_object` + synthetic `submit` tool
- [x] Polymorphic `obs.span` check type (E16)

## 2026-07-16 — Schema / agent UX polish

- [x] Add parameter descriptions to builtin agent tools
- [x] Allow optional `## schema Typename` sections in module markdown for
      `schema(T)` field descriptions
- [x] CLI `.env` load at startup

## 2026-07-16 — P0 coding-agent host gaps

- [x] Runtime `exec.run` + allowlist / `exec.confirm` from `project.json`
- [x] FS host ops: `fs.list`, `fs.edit`, `fs.grep` (agent-tool eligible)
- [x] Language reference card ([language-reference.md](../../language-reference.md))

## 2026-07-17 — Coding-agent example

- [x] Real coding-agent example (`examples/coding-agent`) — universal
      create/fix loop via `llm.agent_object` + FS + `exec.run`
- [x] `fs.find` agent-tool eligible
- [x] Mock fixture `CodingAgentSpec` (empty workspace → Python files + verify)

## 2026-07-17 — Detailed traces

- [x] Per-tool-call spans (`tool:<name>`, `SkAgentTool`) + nested host effects
- [x] `hwfl show` prints compact span attrs
- [x] CLI `hwfl run --debug` (live open/close + end tree)

## 2026-07-17 — Lifecycle tutorial

- [x] Author-facing tutorial: module → check → run → approve/resume → show
      ([tutorial.md](../../tutorial.md))

## 2026-07-17 — Skills phases A–C

- [x] Catalog + check (`skills/`, `skill:` frontmatter, instruction vs callable)
- [x] Host ops `skill.discover` / `skill.load` + project skill budgets
- [x] Agent mid-loop load + snapshot ids + rebuild-from-ids instructions
- [x] Example `examples/skills`; tests SkillCatalogSpec + SkillSpec

## 2026-07-17 — Coding-agent stack skills

- [x] Instruction skills for python / react / haskell / rust under
      `examples/coding-agent/skills/`
- [x] Wire `skill.discover` / `skill.load` into coding-agent toolbox
- [x] Mock fixture discovers/loads python skill before write/verify
