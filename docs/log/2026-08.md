# 2026-08

## 2026-08-11 — Polymorphism next; stdlib via `HWFL_STDLIB`

### Decision

- **Polymorphism before shipping stdlib** — value / let-polymorphism for
  user/stdlib `fun`s is required for a real `hwfl/list.map` (etc.). Effect
  polymorphism stays deferred.
- **Stdlib pack** lives under repo `stdlib/` (qnames `hwfl/…`). At
  check/run, pack root = `HWFL_STDLIB` if set, else a sensible
  install/repo default. Not configured in `project.json`.
- Project `lib/` stays project-local; no `lib/x` → `hwfl/x` rewrite.
  Projects must not claim `hwfl/` qnames.
- Prelude/host remain Haskell; stdlib is ordinary markdown modules
  injected by the loader.

### Docs

- [STATUS.md](../STATUS.md), [TASKS.md](../TASKS.md),
  [stdlib.md](../stdlib.md), [architecture.md](../architecture.md),
  [idea.md](../idea.md), [01-modules.md](../spec/01-modules.md) §3.1.1,
  [03-types.md](../spec/03-types.md) §7.

## 2026-08-11 — Language + interpreter framing

### Decision

- hwfl is a small programming language + durable interpreter with
  first-class LLM and other host effects.
- Near-term work: polymorphism → shipped `hwfl/*` stdlib; short hello
  path (`hwfl init` + tutorial); prefer MCP / stdlib over new host
  categories unless the language cannot express the need.
- Example projects exercise different program shapes and inform the
  language surface.

### Docs

- Updated [idea.md](../idea.md), [STATUS.md](../STATUS.md),
  [TASKS.md](../TASKS.md), root README, docs README,
  [00-overview.md](../spec/00-overview.md), [stdlib.md](../stdlib.md),
  architecture / tutorial notes.

## 2026-08-10 — M-21 exec.run reader hang

- `runCapped` spawns stdout/stderr readers with `forkFinally` so the
  parent always gets a `putMVar` (empty bytes if the reader throws).
- `readCapped` main loop `try`s `hGetSome` (same as drain) and returns
  bytes already read on IO error.
- Regressions in `ExecSpec`: throwing `forkFinally` reader contract,
  timeout under stdout flood within 5s.

## 2026-08-10 — M-20 MCP timeout reconnect

- Transport `Left` from `mcp.call` / `mcp.tools` now
  `invalidateMcpConnection` (drop registry + process-group kill);
  next call reconnects lazily.
- Regression: short `timeout_ms` + slow fixture tool, catch, then
  immediate fast call succeeds (`McpSpec`).

## 2026-08-10 — H-8 MCP spawn allowlist

- `mcp.allow` (basename list) + `mcp.allow_absolute_cwd` (default false).
- Fail closed at `project.json` load (`validateMcpPolicy`) and again at
  spawn (`resolveSpawnSpec`). Dogfood story-writer configs allow `bash`.
- Spec §13 §3 / language-reference updated; optional first-spawn confirm
  deferred (commands are config-time, unlike `exec.run`).

## 2026-08-10 — Bug-report MCP / concurrency follow-up

- Architecture-mapped audit of MCP stdio client, `Runtime/Mcp`, `Exec`,
  store rename, agent human-gate promotion.
- New findings in [BUG_REPORT.md](../BUG_REPORT.md): **H-8** (MCP spawn
  policy ≪ `exec.run` / spec §3 — **fixed** same day), **M-20** (timeout
  leaves wedged cached connection — probe: immediate retry times out,
  recovers after wait), **M-21** (`exec.run` reader `putMVar` hang if
  `readCapped` throws), **L-26** / **L-27**.
- Reconfirmed open: M-3, M-16, M-18.

## 2026-08-10 — Docs sync: MCP shipped

- Cleared stale “MCP planned” hedges in language-reference, overview,
  and [13-mcp.md](../spec/13-mcp.md); acceptance + example-suite E25
  match layered repair / `strict_kb`.
- Spec §6 agent path: document reuse of the host-op apply machine
  (same as `tool(fs.read)`), per the 2026-08-09 implementation note.

## 2026-08-09 — real-story-writer Option claim arms

- LLM bible/extract schemas use `Option<String>` for unused
  `object_entity_id` / `object_lit`; normalize null → `""` before assert.

## 2026-08-09 — real-story-writer chapter deltas claims-first

- Chapter extracts strip aliases (bible owns them); entities only if
  referenced by claims; `try_commit_extract` catches HostErr so MCP
  transport failures become kb_gap instead of aborting the run.

## 2026-08-09 — real-story-writer layered repair

- Chapter ladder: sanitize → commit → extract-only repair → chapter
  rewrite ×2 → outline regen → `kb_gap` + continue; pass-2 over gaps;
  `strict_kb` gates `ok` on full canon commit.

## 2026-08-09 — real-story-writer sanitize+continue

- Chapter extracts: soft schema filter, dead_not_located, one
  status/located_in/bound_to per subject; always write remaining
  chapters if KB commit still fails (no silent commit; prose continues).

## 2026-08-09 — real-story-writer bible harden

- Predicate cheatsheet in bible/extract prompts; local sanitize drops
  KIND_MISMATCH / dead+located_in before assert; up to two LLM regens;
  last-resort minimal alive+located_in seed so chapters can start.

## 2026-08-09 — real-story-writer bible LLM regen

- Bible assert failure no longer trims claims blindly: one `llm.object`
  regen with findings injected (same shape as chapter regen), then
  dry_run/commit; still fail closed if attempt 2 is dirty.
- Prompt locks: dead Person must not also `located_in` (crime victims).

## 2026-08-09 — real-story-writer product demo

- Added [`examples/real-story-writer`](../../examples/real-story-writer)
  beside the MCP fixture: premise → `llm.object` bible → chapter
  outlines → sequential chapter write with `kb_snapshot` continuity
  pack → claim extract → `kb_assert_delta` dry_run → one regen → commit
  only when clean; hard failure stops further chapters.
- **Smoke** (`--example smoke`, no LLM): three-chapter Ashport/Mira
  deltas, planted dead+located clash caught, KB grows
  (`canon-after-ch1` → `canon`). Verified locally `ok=true`,
  `chapters_committed=3`, `contradiction_caught=true`.
- Design locks held: no new host ops; stock `fiction.v1`; workflow
  profile + `kb_snapshot` (not agent `world_pack`); fixture
  `examples/story-writer` left intact.

## 2026-08-09 — story-writer MCP dogfood

- Added [`examples/story-writer`](../../examples/story-writer): durable
  workflow that spawns external **kb-mcp** (stdio) from `project.json`
  (`cwd: workspace`, DB `.kb/story.sqlite`, profile `workflow`).
- **Fixture mode** (no LLM): create/reset `fiction.v1` project → commit
  bible seed → `kb_assert_delta(dry_run)` on planted dead+located clash
  → commit clean ch2 → `kb_snapshot`. Verified locally:
  `ok=true`, `contradiction_caught=true`.
- **Live mode**: `llm.chat` + `llm.object` extract + dry_run / one regen /
  commit (needs provider).
- Companion fixes in kb-mcp (separate repo): `AssertResult` with
  `ok=false` is **not** MCP `isError` (findings are data); tool
  inputSchema allows empty unused claim object arms, scrubbed before
  assert parse — required for hwfl fixed `ClaimRow` records.

## 2026-08-09 — MCP client (stdio) implemented

- Shipped per [spec/13-mcp.md](../spec/13-mcp.md): `Hwfl.Mcp.Client`
  (protocol-only stdio transport: spawn, `initialize`/`initialized`,
  `tools/list`, `tools/call`, per-request timeout, process-group
  teardown) and `Hwfl.Runtime.Mcp` (per-run lazy connection registry,
  config → spawn-spec resolution, `bind` merge / schema stripping).
  `mcp.call` / `mcp.tools` land as host ops (`HostMcpCall` /
  `HostMcpTools`) with `Check.Infer` special-casing `schema(T)` (E14
  pattern) and `mcp.tools`' optional `names` / `bind`, same shape as
  `fs.copy` / `llm.object`.
- **Deviation from spec §6 text:** the spec says agent dispatch of an
  MCP tool should not "open a nested machine … unlike `tool(fs.read)`".
  In practice every host-op tool callee (including `tool(fs.read)`)
  already goes through `startToolCall`'s generic `openApply` +
  `initialMachine` path, which for a single `CurHost` step is a
  one-shot wrapper, not a real CEK evaluation loop — it still gets the
  same span + persist checkpoint as a top-level `mcp.call`. Reusing that
  path for `VMcpTool` (merge `bind`, dispatch to `HostMcpCall`) matches
  the codebase's existing architecture and preserves crash-durability
  semantics; hand-rolling a bypass would duplicate span/persist/pause
  bookkeeping for no behavioural gain. Flagged here instead of silently
  diverging from the spec wording.
- Bug found + fixed while writing the client: `sendLine` never flushed
  the child's stdin pipe (block-buffered by default), so every request
  silently sat in our write buffer until the per-request timeout fired.
- Tests: fixture stdio server (`test/fixtures/mcp/echo_server.py`),
  `Hwfl.Mcp.ClientSpec` (connect/list/call/close, tool-level `isError`,
  timeout, bad command), `Hwfl.Runtime.McpSpec` (`mcp.call` with/without
  `schema(T)`, unknown-server fail-closed, `llm.agent` dispatch via
  `mcp.tools` with `bind` merge + advertised-schema stripping).

## 2026-08-09 — MCP client design (stdio)

- Locked **MCP as client** for external stdio servers (TS knowledge-base
  with ontology session, web search, …): `mcp.call` for deterministic
  RPCs, `mcp.tools` → `List<ToolSpec>` for agents (filter + `bind` for
  handles like `session_id`), optional `schema(T)` on call results.
- Spec: [spec/13-mcp.md](../spec/13-mcp.md); cross-links in host-ops,
  effects, language-reference, architecture, idea.
- Protocol “stateless” = no sticky **protocol** session; app sessions
  remain argument/durable-store handles.
- **hwfl as MCP server** (expose check/run/approve to Cursor) moved to
  TASKS Future / super low priority — distinct from the client; older
  July notes that tied MCP to Servant are not the current priority.

## 2026-08-09 — Typed frontmatter examples + `--example`

- `hwfl check` validates `examples[].inputs` values against frontmatter
  `inputs` types (JSON Schema path shared with tool-arg decode)
- `hwfl run --example <name>` loads a named sample; `--input` overrides
  keys. Fixes L-18.

## 2026-08-08 — coding-agent-chat uses L1+L2; lasting context with `"llm"`

- Example: `context_window = 4`, `consolidate = "heuristic"`,
  `max_rounds = 6`; system prompt mentions `get_history` / `pin`
- Decision: do **not** add lasting pins for heuristic alone (rebuild is
  free). When shipping `consolidate = "llm"`, extend `llm.agent` /
  `llm.agent_object` seed/return with compact state (prefer opaque
  `context`: pins + summary + watermark) so outer history loops do not
  re-pay for overlapping summaries. Update language-reference and
  spec/05-host-ops in that change.

## 2026-08-08 — Context L2 shipped (heuristic consolidate + assemble)

Opt-in L2 on top of L1 (`Hwfl.Runtime.Context`):

- `consolidate = "heuristic" | "manual"` (omit = off); `"llm"` rejected
  with a clear config error until a follow-up
- Auto (heuristic): before each model round, fold
  `[watermark, windowOffset)` into pins + short summary; advance watermark
- Explicit: injected `pin` / `consolidate` tools when mode is on
- Wire assemble = pins + summary + L1 window; `agHistory` unchanged
- Pins / summary / watermark on `AgentState` (snapshot); no format bump

Follow-up: implement `consolidate = "llm"` (extra model round).

## 2026-08-08 — Context L1 shipped (window + history slices)

Implemented in hwfl (not llm-simple `LLM.Agent`):

- Optional `context_window = N` on `llm.agent` / `llm.agent_object`
- Wire turns = last N user turns + follow-ons; full `agHistory` unchanged
- Auto-injected `get_history` when windowing (chunk pages over hidden prefix)
- Wire-side `max_tool_result_chars` (default 16000 when windowed)
- Module: `Hwfl.Runtime.Context`; snapshot fields optional (no format bump)

## 2026-08-08 — Agent context layers (design)

Long-running agents need bounded wire context without losing resume truth.
Implement in **hwfl** (shared helpers / tools once), not by adopting
llm-simple’s `LLM.Agent` window + `get_history` (hwfl already owns the
durable agent loop; `Hwfl.Llm.Simple` only does one-shot generate).

**Two tasks (in order):**

1. **L1 — Window + history slices** — pure window over `[Turn]` (e.g. last
   N user turns); apply before each model round; tool to page older full
   history. Keep full `agHistory` in snapshots. Cap/fold large tool
   results early or windowing won’t help.
2. **L2 — Consolidate + assemble** — extract pins/structured notes from
   spans about to leave the window; compact working history; assemble
   pins + summary + recent window. Separate pin store (workspace and/or
   run-store). Projects pass knobs only.

Transcript ≠ memory ≠ wire context. Do not truncate the only history copy
to match the window.

## 2026-08-08 — L-7: fail-closed section slugs

ASCII `computeSlug` unchanged. Empty slugs (after strip) and duplicate H2/H3
slugs are rejected at module load and `md.sections`, with diagnostics naming
colliding titles — no silent Map last-wins for `@slug`.

## 2026-08-08 — L-13: CLI argv / stale-project exit

Flag parsers moved to `Hwfl.Cli.Args`. `wantsJson` / `flagProviderSet` skip
values of `--workspace`/`--input`/… so `--workspace --json` is not JSON
mode. All positional parsers accept `--` end-of-options for dash-prefixed
paths. Resume hash mismatch is `StaleProjectErr`; `runtimeExitCode` maps it
to exit 4 (no `"stale project"` substring).

## 2026-08-08 — L-11: skill.tags fail-closed

`parseTags` no longer list-comprehends away non-`String` YAML values;
`tags: [1, "a"]` is a diagnostic. Regression in `FrontmatterSpec`.

## 2026-08-08 — L-8: import-cycle path reconstruction

`findCycle` (Kahn remainder) now DFS-walks only edges among remaining
nodes and backtracks; dead-end first-edge paths are no longer reported as
`PceImportCycle`. Regression: first import already sorted, cycle on later
edge; self-import `A → A`.

## 2026-08-08 — L-3: fail-closed snapshot pause / format

`parsePauseReason` no longer falls back to `PauseExplicit` on parse failure;
unknown `reason` strings fail, and legacy reason-less objects still infer
choice vs confirm from shape. `parseSnapshotValue` requires
`snapshot_format == currentSnapshotFormat` (1).

## 2026-08-08 — L-22: case-insensitive find/grep extensions

`matchPat` folds ASCII case so `**/*.md` matches `Notes.MD` (and the reverse)
on case-preserving hosts. Same rule for root-only `*.ext` globs.

## 2026-08-08 — L-1 / L-2: span close discipline

`closeSpan` is idempotent (`ssClosed`) and LIFO-correct: closing a non-head
id auto-closes open children as `SsError` with `unwound=true`. Abort/catch
closes discarded `FrRegion` / `FrInvoke` frames so mid-region failure cannot
leave phantom parents on the stack (or on resume). Agent provider-error /
truncated / submit-required paths no longer pre-close `agent_round`;
`failAgent` owns teardown (optional round close attrs keep usage once).
Host-op errors close the host span before region unwind so snapshot_seq is
not stolen by auto-unwind.

## 2026-08-08 — Bug-fix pass closed; focus → agent substrate

High findings all resolved (H-1 retracted). Medium closed except deferred
M-3 (skill prompt trust), M-16 (multi-process store lock), and M-18
(project-hash / prose resume). Selected Lows fixed (L-1/2/5/6/14/16/19/24);
the rest stay opportunistic hygiene. Active backlog shifts to Tier A agent
ops (MCP, git, persistent terminals). Completed bug IDs archived in
`log/archive/tasks-2026-08.md`.

## 2026-08-08 — L-5 / L-6 / L-14 / L-16: parser and eval footguns

QNames require a tight slash (`a/b`); spaced `a / b` is division. Sequential
`let` still allows an implicit body, but only when it starts on a later line
(`let x = a b` is a parse error). Infix `&&` / `||` desugar to `if` so the
RHS is skipped when short-circuiting. Duplicate record fields are rejected in
the checker (literals, type aliases, patterns, multi-param domains) and
`json.encode` fails closed on duplicate keys.

## 2026-08-08 — M-17: structured tool outcomes and finish_reason

Tool span status no longer depends on prefix-matching result text.
`completeToolCall` takes `ToolOk` / `ToolErr`; call sites set the outcome
explicitly. Provider close attrs include `finish_reason`. Agent rounds with
`FinishLength` fail as `ProviderErr` with an `SsError` round span; stop vs
tool_calls mismatches are advisory attrs only (`finish_reason_mismatch`) —
control flow still follows emitted tool_calls / text. llm-simple still
derives finish reason from tool-call presence (no wire field yet).

## 2026-08-08 — M-12: par agent soft-land / extend

Agent budget exhaustion inside `par` now freezes the pool like confirm/choice/ask
instead of being force-stepped into a "no progress" trap. Branch index is
carried on `AgentExhaustedRequest`; `hwfl extend` bumps that branch's
`agMaxRounds` and resumes scheduling. Legacy `PauseCrashRecovery` branches stay
parked (not runnable).

## 2026-08-08 — M-9: diagnose YAML duplicate frontmatter keys

`YamlSafe`'s libyaml event walk now tracks keys per open mapping and rejects
duplicates (top-level and nested) before `Data.Yaml.decodeEither'` can
last-wins into an aeson `KeyMap`. Wrong module identity / typing from
overridden `name` / `inputs` / `outputs` fields is no longer silently accepted.

## 2026-08-08 — M-10: effect residuals through let-aliases

`inferExprEffects` for `ELet` now inserts the RHS `calleeResidual` into
`EffEnv` and, for alias-shaped RHSs, extends `TypeEnv` via lookup (not a full
re-infer, since fun parameters are not in scope during module effect
analysis). Call-through-alias of a top-level Write fun is charged; a pure
let-shadow is not. Module ceilings can no longer be bypassed by `let g = f in
g()`.

## 2026-08-08 — M-6: exec.run stream caps and process-group timeout

`exec.run` no longer buffers unbounded child output before applying
`max_output_bytes`. Pipe readers retain at most the configured cap and drain
the rest. Children spawn in a new process group; wall-clock timeout sends
SIGTERM then SIGKILL to the group and returns any bytes already captured with
`timed_out`. Negative or overflowing `timeout_ms` / `max_output_bytes` fail at
`project.json` load and again in `runExec`. Docker spawn remains deferred —
group kill covers ordinary grandchildren, not `nohup`/daemonize escapes.

## 2026-08-08 — M-8: resource ceilings on untrusted input

Parse, check, and pure eval no longer rely on GHC stack/heap exhaustion for
hostile modules. Frontmatter YAML is libyaml-event validated before aeson
decode (aliases rejected; nesting/node caps). Expression/type parsers thread a
depth counter. Digit literals parse linearly with a length ceiling; non-finite
floats fail at parse. Pure `eval` is fuel-bounded; CEK crunch also caps frame
depth. `resolveType` memoizes alias expansion so DAG-shaped aliases are
linear in depth. Constants live in `Hwfl.Limits`.

## 2026-08-07 — M-13: ordinary evaluator failures are durable

`runUntilPause` now persists an `MsFailed` root machine before outcome
finalization when a normal machine step returns `Left`. This closes the gap
where `meta.json` was marked failed but `snapshot.json` still held an earlier
machine, allowing a later resume to replay the failed transition. The
regression test verifies snapshot and meta status, then confirms resume returns
the terminal failure.

## 2026-08-07 — M-19: runtime calls use the checked record/Unit ABI

Host operations now normalize a single positional record into named fields
before dispatch, closing the gap where record-domain calls typechecked but
named-only parsers failed. `fs.move` and `exec.run` also accept their checked
positional forms. A lone record now binds by field name to a multi-parameter
function, so packed and unpacked calls agree.

Runtime function tables resolve parameter type aliases throughout top-level and
local functions before evaluation. This makes aliased `Unit` and record
parameters behave like their checker-resolved forms. A single unannotated
parameter now consistently denotes a Unit thunk. Full-suite coverage exercises
the host, record, and alias paths through the machine.

## 2026-08-07 — M-1: model JSON is checked before becoming a workflow value

`llm.object` previously treated `chatResponseFormat` as a provider-only hint:
it decoded the response with `jsonToValue` without checking its schema. That
allowed model output such as `{"score":"high"}` to reach user code where the
checker had promised `{score: Int}`. The object path now reuses
`validateAgainstSchema`, as `llm.agent_object`'s `submit` path already does,
before runtime-value conversion. Schema violations are reported as normal
`HostErr`s, preserving the workflow's checked type contract even when a
provider ignores its format request.

## 2026-08-07 — H-2: a crash is a run outcome, not a process exit

The run loop had no exception barrier, so a provider throw, a full disk during
persist, or a pure exception from H-3/H-4 killed the process while
`snapshot.json` still held the pre-step machine — a resume would then replay a
transition whose side effects had already happened (double `exec.run`, double
LLM spend). Contained at three boundaries, all on `Hwfl.Exception.trySync`:
`runHostOp` (→ `HostErr`, catchable), `safeLlmChat` (→ `ProviderError`, used by
all four model call sites), and `guardedStep` around every `stepMachine` call
in `runUntilPause` (→ `InternalErr`). The step barrier closes the spans the
crashed step opened and persists `MsFailed` before returning, so the durable
state matches what actually ran. `app/Main.hs` grew a last-resort handler so
anything escaping outside a run is still a `--json` error envelope.

### Decisions

- **Contained means synchronous only.** `trySync` re-throws
  `SomeAsyncException`, `ExitCode`, `ThreadKilled`, `UserInterrupt` and
  `HeapOverflow`: those are a request for this thread to stop, and swallowing
  them would break Ctrl-C, `exitWith`, and any future cancellation from the
  control plane. `StackOverflow` is deliberately treated as a run failure —
  deep author recursion is a program error and GHC has already unwound.
- **A crash is not catchable by author code.** `InternalErr` is a new
  `RuntimeError` constructor with `isCatchable = False`. A host op that throws
  is still `HostErr` (the op boundary is well-defined, so `try`/`catch` works
  as before), but an exception that escapes a *step* aborted at an unknown
  point, possibly after applying part of its effect; letting a workflow
  continue from there would be worse than failing.
- **Nested branch machines are not barriered separately.** Agent tool calls,
  `par` branches and `FrInvoke` nests call `stepMachine` directly, so their
  crashes reach the enclosing `runUntilPause` and fail the whole run rather
  than being recovered as "that tool call failed, keep going".
- Cleanup on the crash path (span close, persist) runs under its own
  `trySync`, so a store that also fails cannot replace the original error.

## 2026-08-07 — H-7: run ids are store keys, not path fragments

Run ids reached `workspace/.hwfl/runs/<id>` verbatim, so a library caller (or
`hwfl resume . ../../pwned`) could create directories and read/write
`meta.json` / `snapshot.json` anywhere the process can write. `validateRunId`
now accepts only a single portable path component (`A-Za-z0-9._-`, no leading
`.`, ≤ 128 chars), and the sole id→path mapping (`runDirFor`) validates first,
so a new backend cannot reintroduce the hole by joining an id itself.

The entry points collapsed to two: `createRun` for a start, `openRun` for a
continue. `openRunStore` / `tryOpenRunStore` are gone — they created the run
directory on the way in, which is why an unopenable run (unknown id, stale
project hash) still left a directory behind. Failure paths now take a pure
handle (`runStoreHandle`, creates nothing) or a detached one (no directory at
all: writes dropped, reads empty), so reporting an outcome for a run that was
never opened has no filesystem effect.

### Decisions

- **Starting a run is create-only.** `createRun` uses `createDirectory`, not
  `createDirectoryIfMissing`, so an id already in use is rejected without a
  check/create race. The report suggested an "unless explicitly intended"
  opt-in for reuse; there is no correct reuse semantics — the old snapshot and
  spans survive while meta is replaced and the sequence restarts, which is the
  exact corruption being fixed — so continuing an existing run stays the job of
  resume / step / approve, and no opt-in flag was added.
- Untrusted-input treatment applies to run ids, not just filesystem paths:
  they arrive from the control-plane API and from workflow code via
  `meta.read_spans` / `meta.read_snapshot`.
- A `RunStore` handle may be detached (`storeRoot :: Maybe FilePath`).
  Backend writes are no-ops on it rather than errors, so error paths can carry
  a store without a directory.

## 2026-08-07 — H-1 retracted; real escape was create-before-check (H-1a)

H-1 claimed `fs.write` / `fs.copy` followed dangling symlinks out of the
workspace. Attempting to write a proof-of-concept disproved it:

- `directory`'s `canonicalizePath` is not `realpath`. `realpathFurther`
  dereferences unresolved segments via `getSymbolicLinkTarget`, so a dangling
  link canonicalizes to its resolved-but-missing target. The old leaf check
  therefore saw an out-of-root path and returned `SandboxErr`.
- `copyFile` is temp-file + `copyPermissions` + `rename`, and `rename` never
  follows a symlink on the final component, so the copy replaced the link
  in place. Never an escape.

Replaying the HEAD~1 logic against dangling and live out-of-workspace links
blocked all four cases. The report entry is retracted with the measurements.

**The genuine bug found while disproving it (H-1a):** every mutating op ran
`createDirectoryIfMissing` and *then* checked containment, so
`fs.mkdir("dirlink/escaped")` through an in-workspace directory symlink
created the directory outside the root before failing. Fixed by
`ensureDirUnderRoot`, which descends one component at a time from the
canonical root and checks before each `createDirectory` — the cursor is
canonical and contained at every step, so nothing outside can be created.

Also fixed, none of it in the original report:

- `fs.remove` on a *directory* symlink canonicalized and then
  `removeDirectoryRecursive`'d the **target**. Silent data loss outside the
  sandbox; now the link is unlinked. The worst bug in this area.
- `fs.copy` had lost atomicity and permission preservation when it stopped
  using `copyFile`; restored via `O_EXCL` temp + `rename`, and a
  file-onto-file overwrite is no longer pre-removed, so the destination
  survives a failed copy.
- `fs.exists` and `fs.stat` disagreed on dangling links (`true` vs a hard
  error). A symlink is now consistently an existing entry with
  `kind = "symlink"`.
- Leaf symlinks are followed when they resolve inside the workspace, so
  writes are symmetric with reads and internal aliases keep working.
- `ELOOP` detection moved from matching the locale-dependent `strerror`
  string to `ioe_errno`.

### Decisions

- Mutating ops never trust a path that was validated before use: containment
  is checked before each component is created, and the final open is
  `O_NOFOLLOW` (or a `rename`, which cannot follow). Check-then-use is only
  acceptable when the use itself is symlink-proof.
- Sandbox policy is symmetric: a symlink is followed iff it resolves inside
  the workspace, for reads and writes alike.
- A symlink is a directory entry, not its target: it exists, it stats as
  `"symlink"`, and removing it does not touch what it points at.
- `[Verified]` in the bug report means "code read", not "failure reproduced".
  Security findings need an executed PoC before they are acted on; H-1 cost a
  round of regressions because a library contract was assumed rather than run.
- `Workspace` is POSIX-only (`unix`: `O_NOFOLLOW`, `lstat`, `fchmod`). No
  Windows support is planned; revisit with CPP if that changes.

## 2026-08-07 — H-6: function-call shapes preserve checker/runtime agreement

An empty call now rejects every non-`Unit` function domain instead of returning
the function type. Runtime binding supplies `VUnit` for an omitted `Unit`
parameter, so successful `f()` calls have the same interpretation at both
layers. A one-parameter record function now packs individually supplied
positional or named fields into that record; multi-parameter functions retain
their existing positional/named binding. `fs.write` also accepts its
two-position argument form, which the checker has always validated.

### Decision

- A record domain represents both the type of multi-parameter functions and a
  single record parameter. Its runtime `Param` metadata resolves the ambiguity:
  one annotated record parameter receives a constructed record, while multiple
  parameters bind individually. This preserves the concise multi-argument
  language surface without an invasive callable-shape annotation in `TypeExpr`.

### Follow-up

Post-fix review kept H-6 closed and filed **M-19** for the leftovers: host
ops that still parse only named fields while the checker unpacks record
domains; whole-record calls to multi-parameter functions (same `TypeExpr`
ambiguity in the other direction); and Unit/record packing that keys off
syntactic annotations rather than resolved types.

## 2026-08-07 — H-3: JSON numbers retain their mathematical value

`jsonToValue` previously narrowed every JSON `Scientific` number to `Double`
before deciding whether it was an integer. It now uses `floatingOrInteger`:
integral values go straight to arbitrary-precision `VInt`; non-integral values
use the runtime's `Double` `VFloat` representation and reject a non-finite
conversion. The error flows through `llm.object`, agent submit/tool arguments,
and meta-read values as a normal failure. Regression coverage includes the
first non-representable integer above 2^53 and nested values.

## 2026-08-07 — H-4: non-finite Float is a language error

Float literals and arithmetic now construct values through `finiteFloat`, which
rejects NaN and Infinity as `Trap`s. Overflow therefore stops before the
machine can persist an invalid value. JSON encoding is now recursively fallible
and string interpolation likewise rejects non-finite values, protecting those
boundaries against a malformed value introduced by future internal code. Agent
tool-result serialization converts that boundary failure into its existing
recoverable tool-error path.

### Decision

- **Finite `Double` is a runtime invariant.** IEEE special values are neither
  JSON values nor valid snapshot payloads, so they must not be represented by
  `VFloat`. Serialization checks remain as a defense-in-depth boundary, not a
  lossy conversion to JSON strings or `null`.

## 2026-08-07 — H-5 / L-19: bounded agent round budgets

`nextPow2` previously enumerated powers of two in `Int`; after the sign bit,
the sequence wrapped negative and the exhausted-agent hint loop never
terminated. It now doubles with a pre-overflow guard and saturates. Suggested
extensions are also capped to the remaining `Int` headroom, yielding zero when
no further representable extension exists.

Source `max_rounds` rejects positive integers that cannot fit `Int`; persisted
agent state rejects non-positive budgets and counters. Extending a paused agent
now rejects non-positive increments and checks addition before changing the
machine, so neither source nor resume paths can wrap the budget.

## 2026-08-07 — M-5: file discovery does not follow directory links

`fs.find` and glob-less `fs.grep` had independent recursive walkers that
tested `doesDirectoryExist` first. Because that follows links, a workspace
directory link could walk `..`, `/`, or itself. They now share `walkFiles`,
which checks link identity before classifying a directory, never descends a
directory symlink, canonicalizes each actual directory beneath the workspace
root, and tracks canonical directories already visited.

The regression test covers an external directory, an internal directory alias,
and a self-loop. Only the ordinary in-workspace file is returned by either
operation. The full `hwfl-test` suite passes.

## 2026-08-07 — M-14: sentence splitting retains trailing text

`splitSentences` previously discarded its in-progress buffer whenever it had
already emitted a sentence, losing the final unpunctuated sentence. Its
end-of-input case now appends a non-blank buffer after completed sentences.
Regression coverage includes punctuated and unpunctuated endings, a single
fragment, and whitespace-only input.

## 2026-08-07 — M-2 / M-11(b): secret taint survives model JSON

`schema(Secret<T>)` now emits a runtime-only `x-hwfl-secret` annotation.
Provider-bound schemas recursively remove that annotation, while `llm.object`
and agent `submit` validate then decode the response structurally, restoring a
`VSecret` at every marked node. The annotation survives a paused machine
snapshot as part of its `VSchema`, without requiring a type environment at
host-call time.

Redaction now normalizes sensitive key names, recursively handles JSON encoded
inside text, and recognizes PEM blocks, JWTs, provider-style keys, and
credential-shaped hex. Stream event fields are redacted before append and the
stderr observer plus host-progress writer apply the same final scrub.

### Decision

- Shape heuristics cannot soundly identify an arbitrary short secret in a plain
  string. `VSecret` remains the confidentiality boundary; text redaction is
  deliberately defence in depth for external/untyped content.

## 2026-08-07 — M-7 / M-15: source reads are explicit and contained

`Hwfl.SafeIO` centralizes byte reads behind stable English error categories.
Module loading now decodes explicit UTF-8 bytes, so missing, unreadable, and
non-UTF-8 source becomes a normal parse diagnostic rather than a raw
`IOException`. Project configuration and module-directory traversal use the
same containment boundary.

Model pricing now distinguishes a deliberately absent catalog (allowed for
mock-provider runs) from an unreadable or malformed existing catalog. The
latter is a surfaced `ConfigErr`, preventing an invisible switch to zero cost
attribution. Full `hwfl-test` coverage passes.

## 2026-08-08 — M-4: keep every system message in the provider prompt

`requestToTurns` previously kept only the first system text (`chatSystem` or
the first `RoleSystem`), silently dropping the rest on the
`llm.chat_messages` path. It now joins all non-empty system segments with
`\n\n`, skips re-adding a Host-prepended `chatSystem` duplicate, and leaves
the agent `chatTurns` path unchanged.

## 2026-08-08 — M-11(a): Option schema / JSON interop

Record JSON Schemas omit `Option` fields from `required` (including aliases
that resolve to `Option`). `schema(Option<T>)` now carries a runtime-only
`x-hwfl-option` marker (stripped for providers). Schema-directed decode maps
JSON `null` and absent optional fields to `None`, and present values to
`Some(_)`, instead of collapsing `null` to `VUnit`.

Language surface: `None` / `Some(e)` constructors (`ETag`) with bidirectional
checking, and `match` arms that bind `Some` payloads to the inner type.
Regression coverage includes schema reflection, validation omit/`null`, pure
eval, and `llm.object` end-to-end.

### Decision

- Represent Option as `VVariant` `Some`/`None` (same path as TagPat), not a
  dedicated value constructor, so match/eval stay unified.
- Prefer an explicit `x-hwfl-option` annotation over inferring Option from
  structural `anyOf`+null alone (`Unit` is also JSON null).
