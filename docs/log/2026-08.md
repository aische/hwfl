# 2026-08

## 2026-08-07 — H-1 sandbox write/copy leaf symlink fix

- Write and copy destinations open with `O_NOFOLLOW` so dangling leaf
  symlinks cannot redirect outside the workspace.
- `pathExists` treats leaf symlinks as workspace dirents (parent
  containment only); `removePath` unlinks them so overwrite copy works.
- Closes L-24 (TOCTOU leaf follow + dangling remove) with H-1.
