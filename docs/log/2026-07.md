# Decision log — 2026-07

## 2026-07-15 — Greenfield language direction

- Restart computation substrate as a **minimal ML** embedded in markdown
  modules; keep hwfi as a side-by-side reference, not the forever host of
  new features.
- Durable resume stays at **host-op / effect boundaries**, not every pure
  reduction (same transition idea as hwfi v2 machine).
- Default LLM library remains **llm-simple**, but workflows must not depend
  on it: introduce an internal **`LlmProvider`** adapter from day one.
- Spec lives as a multi-file pack under `spec/`; project workflow mirrors
  hwfi (`STATUS` / `TASKS` / monthly log / Cursor handoff).
- Provisional product name: **hwfl**.

## 2026-07-15 — M0 syntax skeleton landed

- Cabal package `hwfl` (GHC2021): kernel AST/parser/pretty + markdown loader.
- Surface choices locked for v0 parse: sequential `let` without `in`,
  record field shorthand `{ name }`, host paths via `.` projection
  (`llm.chat`), module paths via `/` qnames; `null` literal not in kernel
  (Option/Result preferred per language spec).
- `LlmProvider` / llm-simple deferred to M4 (acceptance), not wired in M0.

## 2026-07-15 — M1 pure evaluator

- Big-step recursive pure eval (`Hwfl.Eval.Pure`), not an explicit CEK loop:
  matches runtime §2–3 (no snapshot mid pure reduction). Frame/CEK driver
  deferred until host transitions need resumable konts (M4+).
- Infix `+ - * /`, comparisons, `&&`/`||` elaborate in the parser to
  `EApp (EVar op) […]`; op idents are prelude `VBuiltin`s (not host ops).
- Top-level `fun` decls knot-tied into one shared env (mutual call OK).
- `par` / `join` / `confirm` / `try` / `@section` / bare qnames →
  `Unsupported` (trap if reached). No HostRef / Bytes yet.
- Arithmetic same-sort only (Int+Int or Float+Float); Int `/` is `div`.

## 2026-07-15 — M2 type checker

- Bidirectional check (`Hwfl.Check.Infer`) over resolved aliases; effects on
  arrows are stripped (lattice deferred to M3).
- Module frontmatter inputs/outputs elaborate missing `main` annotations and
  are compared to the inferred `main` type.
- Host op _types_ for `fs.read` / `llm.chat` live in check prelude so bodies
  like summarise type-check before M4 runtime; no effect enforcement yet.
- `schema(T)` is a parse form (`ESchema`); check-time JSON Schema for
  records/lists/bases; eval traps (compile-time operator).
- Prelude `+`/`==`/ord typed as Int for v0; Float polymorphism later.
- Record equality is by field name (order-insensitive); no width subtyping.

## 2026-07-15 — M3 effects + check CLI

- Residual effect inference (`Hwfl.Check.Effects`) over apps of `TEffFun`
  host stubs plus `par`/`join`/`confirm`; module `effects:` is a hard ceiling
  (absent ⇒ pure; inferred ⊈ declared ⇒ `EffectsNotAllowed`).
- `hwfl check <module.md>` loads + type-checks + effect-checks one file; exit 1
  on failure, exit 2 on usage. Project.json + import graph deferred (same
  command later) — logged on stderr in single-module mode.
- E12 fixture fails closed; summarise with `[Read, Net]` passes. No M4 host
  runtime / provider yet.

## 2026-07-15 — M4 host + LlmProvider + snapshots

- Runtime host ops mirror check prelude only (`fs.read`/`fs.write`/`llm.chat`);
  no second API surface. Pure eval still traps on `VHostOp`.
- Workspace sandbox: lexical resolve + canonicalize prefix (symlink escape hard
  fail). FileRef at runtime is a path string under the workspace.
- `LlmProvider` record in `Hwfl.Llm.Provider`; engine types in `Hwfl.Llm.Types`.
  Default adapter `Hwfl.Llm.Simple` wraps llm-simple (`^>=0.1.0.1`); retries stay
  in the provider. Mock provider for CI; `--llm-provider mock|simple` and
  `HWFL_LLM_PROVIDER`. Workflows never import llm-simple.
- Snapshots: after each completed host op write format-1 boundary record under
  `.hwfl/runs/<run-id>/snapshot.json` (+ `transitions.jsonl`). Full kont /
  `machine_json` for `hwfl resume` deferred to M5 (logged; M4 establishes the
  host-boundary resume points only).
- `hwfl run <module.md> [--workspace] [--input k=v…]`: check then execute `main`;
  human logs on stderr; exit ≠0 on failure. Project graph still single-module.
- E03/E04 mock-green; sandbox escape rejected; 66 tests. Real network LLM not
  required in CI.

## 2026-07-15 — M5 concurrency + human + resume

- Runtime switched to an explicit machine (`Hwfl.Runtime.Machine`): frames +
  `Current`, big-step pure crunch, small-step host/par/confirm transitions.
  Top-level funs are `VTopFun` so snapshots stay finite (no knot-tied env).
- `par(max)` scheduler with ordered slots; confirm inside a branch drains
  in-flight work then pauses (`awaiting_confirm`); completed slots not re-run.
- `confirm` sugar and `human.confirm` host op share the same pause gate;
  `hwfl approve --yes|--no` injects `Bool` and continues.
- Snapshots include full `machine_json` (+ `meta.json` entry path / hash).
  CLI: `step` / `resume` / `approve`; `hwfl run --step`; exit 3 pause, exit 4 stale.
- E07 ordered par, confirm+approve, par-confirm freeze, step/resume, stale hash
  covered; 71 tests. OS-thread parallelism deferred (cooperative pool is M5).

## 2026-07-15 — M6 observability + show

- `Hwfl.Obs.{Span,Trace,Redact,Show}`: append-only `spans.jsonl` open/close lines
  (O(1) per transition; tree built only by `hwfl show`, never on the step hot path).
- Every host op + module `main` emit nested spans; `human.confirm` keeps a span
  open across pause and closes on approve. `obs.log` / `obs.span` wired (region
  via `FrRegion` / `CurCloseRegion`). Check-time `obs.span` return type is still
  Unit→Unit (polymorphism deferred).
- Redaction: `VSecret` never cleartext in snapshots; host attrs store path/model
  and string lengths (not prompt bodies); sensitive JSON keys scrubbed on write.
- Snapshot persists `span_stack` + `span_counter` so resume keeps nesting.
- CLI: `hwfl show <ws> <run-id> [--tree|--spans|--snapshot] [--filter PREFIX]`.
- 74 tests (redaction + summarise-style span tree).

## 2026-07-15 — M7 agent loop + typed tools

- `llm.agent` is a multi-transition machine current (`CurAgent`), not one opaque
  host call: model round → sequential tool nested machines → next model / final
  `{ text, rounds }`. Each model/tool boundary snapshots (resume mid-tool OK).
- Prelude `tool(f)` advertises host ops (`fs.read`/`fs.write`) or typed module
  funs/closures with JSON Schema from param annotations; Infer special-cases
  `tool` so any function type → `ToolSpec`.
- `LlmProvider` `ChatRequest` gains tools + turns; adapters map to llm-simple
  `ToolDef` / content blocks. Agent rounds emit `agent_round` child spans under
  the open `llm.agent` host span.
- No llm-simple agent loop in-engine — hwfl owns stepping (same idea as hwfi
  MachineAgent). Skills / `llm.agent-object` submit tool deferred.
- E15 green; 76 tests.

## 2026-07-15 — M8 semantic-check dogfood

- Ported layers 0–2b as **one** hwfl module (`examples/semantic-check/workflows/main.md`,
  ~300 LOC) vs hwfi’s **74** tool files + workflow (~3175 LOC) — about **75×**
  fewer author files and **~10×** fewer LOC. Fitness target (≤15 modules) met.
- Unblocks for in-language review (not a second DSL):
    - Pure prelude: `list.length` / `list.concat` (recursive libs without cons yet),
      `text.{metrics,similarity,contains,split_sentences,words,strip_suffix}`,
      `md.sections`
    - Host: `fs.find`, `meta.check_module` (recoverable `{ok,error,name}`)
    - Check: `String`≅`FileRef` for path literals; `==`/`</>` on String/Float
- Decision: keep policy in the workflow; keep metrics pure (not host snapshot
  boundaries). Defer list rest/`::` patterns; `length`+`concat`+index recursion
  is enough for E20-style folds. Project import graph still deferred — dogfood
  intentionally packs helpers in one module.
- E20 fixture green; 78 tests.

## 2026-07-15 — Docs sync (post-M8)

- `spec/05-host-ops`: `meta.check_module` + pure prelude `list`/`text`/`md`;
  `meta.check_project` marked deferred.
- `spec/10` v0 gate: M0–M8 complete; ongoing fitness metrics unchanged.
- `spec/12` E20, `idea.md`, `hwfi-reference`, `TASKS` Now/Later, bootstrap
  README/summarise example brought in line with shipped code.

## 2026-07-15 — Post-M8 priority / doc sync

- **Next engineering:** project-wide `hwfl check` (`project.json` + import graph).
  M3 deferred this; M8 dogfood stayed one module intentionally.
- **After check:** Float/`==` cleanup → `llm.object` (E14) → `llm.agent-object`
  with submit tool.
- **Deprioritized:** alternate `LlmProvider` (OpenAI/Anthropic SDK direct). The
  `LlmProvider` record + llm-simple + mock are sufficient; second adapter is
  interface proof only, not a capability gate.
- Semantic report fix: JSON via `json.encode`, path `.hwfl/runs/<run-id>/semantic-report.json`,
  runtime `ctx` binding on fresh runs.
- Updated `TASKS`, `STATUS`, `spec/10` release gate accordingly.

## 2026-07-15 — M9 project-wide `hwfl check`

- `Hwfl.Project`: load `project.json` (entrypoint, effects default/deny, exec
  policy), discover modules under `workflows|lib|tools|types` → qnames.
- `Hwfl.Check.Project`: import graph from frontmatter, cycle rejection, topo
  check of reachable modules with import export records for `lib/foo.bar`.
- CLI: `hwfl check <project>` / `hwfl run <project>` (still supports single
  `.md`); `meta.check_project` host op + snapshot codec.
- Fixtures: `test/fixtures/check-project`, `check-project-cycle`; 81 tests.
- Docs: STATUS, TASKS, acceptance M9, host-ops.

## 2026-07-15 — Float / `==` overloading cleanup

- New `Hwfl.Check.Overload`: applications of `+ - * /`, `== !=`, ord ops typed
  by operand sort (replaces M8 Infer special-cases + Int-only prelude lie).
- Rules: arith same-sort Int|Float (no String `+`); eq on comparable bases +
  structural List/Record; ord on Int|Float|String|FileRef.
- `String` ≅ `FileRef` kept as dedicated **path coercibility** in
  `typesCompatible` (arg passing + eq operands) — not general subtyping.
- Bare overloaded ops rejected (no principal type); Effects skips them as Pure.
- Runtime `valueEq` / arith / ord documented to match; 94 tests; semantic-check
    - project-check green. Spec §03 / stdlib updated.
- Next: `llm.object` (E14) → `llm.agent-object`.

## 2026-07-15 — `llm.object` (E14)

- Host `llm.object`: named args `prompt` / `schema` / `model`; schema is a runtime
  `VSchema` from `schema(T)` (machine eval uses module `TypeEnv` aliases).
- `ChatRequest.chatResponseFormat` is `Maybe Value` (JSON Schema); Simple adapter
  routes to llm-simple `genObjectUntyped`; mock fills placeholder objects from the
  schema for CI.
- Infer special-cases `llm.object(..., schema = schema(Out), …)` ⇒ result type
  `Out` (prelude stub remains `Json` when schema is opaque).
- Decode: provider JSON text → `jsonToValue`. E14 fixture green; 97 tests.
- Next: `llm.agent_object` + synthetic submit tool.

## 2026-07-15 — `llm.agent_object` + submit tool

- Host `llm.agent_object` (surface underscore — idents disallow `-`): same
  multi-transition `CurAgent` loop as `llm.agent`, plus `schema = schema(Out)`.
- Infer: result `{ value: Out, rounds: Int }` when schema is `schema(Out)`.
- Engine injects synthetic `submit` tool (hwfi §6.1.3): call ends the loop with
  decoded args as `value`; plain-text finish is fatal; mixed submit+other rounds
  recover with feedback and run no tools; decode failures are recoverable.
- Snapshot persists `submit_schema`; 100 tests; example `examples/agent-object.md`.

## 2026-07-15 — Polymorphic `obs.span` (E16)

- Infer special-cases `obs.span` so full apps return the thunk body type:
  `obs.span(name)(fun () => e)` / `obs.span(name, fun () => e)` ⇒ type of `e`
  (effects of `e` still collected via Effects walking the thunk).
- Prelude stub remains `String -> (Unit -> Unit) -> Unit` for bare/partial uses.
- Runtime: region open/close unchanged; small curry coalesce so the first
  `obs.span(name)` application waits for the thunk on `FrAppFun` (no new host op).
- Checker: empty `fun ()` param lists bind as `Unit -> a` (`paramBindings`).
- 102 tests; example `examples/obs-span.md`.

## 2026-07-16 — Builtin agent-tool parameter descriptions

- `tool(fs.read)` / `tool(fs.write)` now emit JSON Schema property
  `description` fields for their builtin parameters, so provider-facing tool
  schemas are more specific without changing call semantics.
- Kept the implementation local to `Hwfl.Runtime.Agent` rather than baking docs
  into general `schema(T)` yet; that broader field-doc path remains the next
  task via markdown-backed schema sections.
- Added a regression test that asserts the exact advertised schema for both
  builtin agent tools.

## 2026-07-16 — Markdown-backed schema field docs

- Added optional `## schema Typename` sections to markdown modules. Their
  bodies are parsed as simple markdown bullets like `- field: description`,
  with wrapped continuation lines preserved into the stored description text.
- `LoadedModule` now carries parsed schema-doc entries, and runtime
  `schema(T)` reflection enriches named alias record fields with JSON Schema
  `description` properties when matching docs exist.
- Kept docs attached to named aliases only, rather than anonymous inline
  records, to avoid introducing a second naming scheme for structural types.
- Added loader and schema-reflection regressions; full suite passes at
  104 tests.

## 2026-07-16 — Skills implementation plan (docs only)

- Captured a full skills design in [skills-plan.md](../skills-plan.md):
  two kinds (`callable` / `instruction`), checked project catalog,
  `skill.discover` + `skill.load`, agent mid-loop tool/context mutation,
  budgets, resume fields, phased delivery (catalog → ops → agent → optional
  extraction).
- Explicitly not a substitute for `fs.read`: load must advertise tools or
  inject instruction context and remain resume-correct.
- Behaviour aligned with hwfi §6.6–§6.7 / `skills-design.md`; surface names
  use hwfl `skill.*` host ops. Implementation deferred (TASKS Later).

## 2026-07-16 — Post-M9 backlog priority reorder

- Reordered active work toward **coding-agent readiness** over streaming /
  DB / skills / HTTP:
  1. Host gaps: runtime `exec.run` + richer FS (`list` / `edit` / `grep`, …)
  2. Language reference card, then coding-agent example + tutorial + `--debug`
  3. Streaming LLM spans; semantic-check deepen
  4. Skills → run-store interface → optional DB → Servant → MCP
- Constraints locked in planning: DB replaces **run persistence**, not the
  workspace sandbox; Servant/MCP sit on the same APIs as the CLI; do not
  start MCP or Servant before a run-store boundary and a real coding-agent
  dogfood.
- CLI `check` / `run` / `step` / `resume` / `show` and
  `examples/semantic-check` remain done — not re-planned as greenfield.
- `TASKS.md` rewritten; M0–M9 Done list archived to
  [archive/tasks-2026-07.md](archive/tasks-2026-07.md).

## 2026-07-16 — P0 host gaps (`exec` + FS + langref)

- **`exec.run`:** `Hwfl.Runtime.Exec` — basename allowlist, env allowlist,
  timeout / max-output from `project.json`; returns
  `{ exit_code, stdout, stderr, timed_out }` (non-zero exit is a value).
  New `exec.confirm` (default `true`) pauses via the existing approve gate
  before spawn (`FrAfterConfirm` / `FrExecApproved`).
- **FS:** `fs.list` → `{ name, kind }[]`; `fs.edit` literal replace →
  `{ ok }`; `fs.grep` regex + optional glob → `{ file, line, text }[]`.
  Agent `tool(...)` metadata + arg coercion for the new ops.
- **Docs:** [language-reference.md](../language-reference.md) one-card
  keywords / types / prelude / host signatures.
- Check prelude return type for `exec.run` updated from `Unit` to the
  capture record (examples using `let _ =` remain valid).

## 2026-07-17 — Coding-agent example

- Shipped `examples/coding-agent`: one `llm.agent_object` module that takes
  `prompt` + `model`, explores the workspace, writes/edits files, runs
  allowlisted toolchain commands, and submits
  `{ summary, ok, stack, files_written, verify_exit }`.
- Workspace is separate (`--workspace`); empty dir or existing project.
  `fs.write` creates parent dirs — no `mkdir` host op required for greenfield.
- `project.json` allowlists common basenames (python/npm/cabal/…) with
  `exec.confirm: false` for demos; set `true` for operator approve gates.
- Made `fs.find` agent-tool eligible (was host-only).
- Fixture: `CodingAgentSpec` scripts mock LLM to create Python in an empty
  temp workspace and `exec.run python3` verify.

## 2026-07-17 — Detailed run traces

- Agent tool calls open `tool:<name>` spans (`SkAgentTool`) with redacted /
  truncated argument attrs; host effects remain nested child spans.
- `agent_round` stays open across the tool phase so the tree nests
  `round → tool → fs.* / exec.*`.
- `hwfl show` prints compact attrs on the span tree / `--spans` lines.
- CLI `hwfl run --debug`: live `span open` / `span close` on stderr, then
  the full tree after the run (via `showStore`).

## 2026-07-17 — Lifecycle tutorial

- Shipped [tutorial.md](../tutorial.md): module anatomy, project vs
  workspace, `check` / `run` / `show`, confirm → `approve` / `resume`,
  `--step` / `--debug`, exit codes. Uses `examples/obs-span.md` and an
  inline confirm module; points at coding-agent and skills-plan for later.

## 2026-07-17 — Skills phases A–C

- **Catalog + check:** `skills/` in discovery; nested `skill:` frontmatter;
  instruction skills (no `hwfl` fence); `Hwfl.SkillCatalog` at check time;
  `project.json` skill budgets.
- **Host ops:** `skill.discover` / `skill.load` (Meta+Read); recoverable
  `{ ok = false, … }`; outside-agent callable load is metadata-only.
- **Agent mid-loop:** special-case `skill_load` mutates
  `active_tool_ids` / `loaded_instruction_ids`; instruction context
  **rebuild-from-ids** at model round; callables advertise via `VSkillMain`.
- Explicit meta-tools only (authors list discover/load). No `hwfl skill list`.
- Example: `examples/skills`. Tests: SkillCatalogSpec + SkillSpec (120 total).
- Deferred: phase D extraction writer; wiring skills into semantic-check.

## 2026-07-17 — Coding-agent stack skills

- Extended `examples/coding-agent` (not a fork): thin system prompt +
  `tool(skill.discover)` / `tool(skill.load)` + FS / `exec.run`.
- Instruction skills: `python-pytest`, `react-vite`, `haskell-cabal`,
  `rust-cargo`. Agent discovers/loads before writing.
- `CodingAgentSpec` mock loads `skills/python-pytest` then builds the
  Python fixture. Skills acceptance §10 “no hardcoded callables” closed
  via this dogfood.

## 2026-07-17 — Skills docs sync

- Promoted skills out of defer/planned in specs `00` / `01` / `05` / `06` /
  `10`, plus `architecture`, `idea`, `README`, `tutorial`, and
  `language-reference` (`skill.discover` / `skill.load`).
- `skills-plan.md` remains design + acceptance; normative sketches now
  live in the numbered specs.

## 2026-07-17 — `par` concurrency docs (cooperative vs future)

- Clarified M5 `par` is a **cooperative** pool: ordered slots, confirm
  drain, and resume work; one branch transition at a time — blocking host
  ops do not overlap.
- Documented future nice-to-have: **concurrent host transitions** via
  async at host boundaries, coordinator-owned spans/snapshots (not raw
  OS-thread-per-branch). Spec [06-runtime.md](../spec/06-runtime.md) §10;
  backlog in [TASKS.md](../TASKS.md) (low priority).
- Terminology: “OS-thread parallelism” in the M5 log meant “no GHC
  concurrency layer yet,” not a requirement for pthread-style threading.

## 2026-07-17 — `--json` CLI errors

- `Hwfl.Cli.Json`: thin error envelopes on stderr for `hwfl check` / `hwfl run
  --json` (`status`, `exit_code`, `category`, `kind`, `message`; parse
  `diagnostics` array). Success output unchanged; `--output json` remains
  separate (spec §09 recommend).

## 2026-07-17 — Coding-agent capability backlog (Tier A/B)

- Captured future / nice-to-have items for a Cursor-class **headless** agent
  in [TASKS.md](../TASKS.md): Tier A (`fs.patch`, git ops, persistent
  terminal, context pre-pass) and Tier B (codebase index, LSP, rules/hooks,
  auto context assembly, multi-model routing).
- Already-tracked pieces stay where they are (streaming, MCP, `meta.invoke`,
  concurrent `par` host IO, run-store / Servant). Prefer MCP and workflow
  modules over growing host ops.
- Tier C (IDE / product shell) explicitly out of scope for hwfl.

## 2026-07-17 — Streaming LLM spans (design lock)

- Progressive token / partial text is an **observability side channel**
  only: provider `on_chunk` → coalesced `events.jsonl` on the open
  `llm.chat` / `agent_round` span; optional coalesced `--debug` echo.
- Host ops stay atomic (full return value, one transition). No
  author-facing stream types; no mid-token snapshots; crash mid-stream
  still re-runs the whole call.
- In scope: text + agent model rounds via `llm-simple`
  `streamTextWithFallbacks`; mock fake chunks for tests.
- Out of scope for this item: `llm.object` streaming, tool-arg fragment
  streaming, `par` concurrent host IO.
- Spec: [07-observability.md](../spec/07-observability.md) §9,
  [08-llm-provider.md](../spec/08-llm-provider.md) §2.2,
  [05-host-ops.md](../spec/05-host-ops.md) LLM notes,
  [09-cli.md](../spec/09-cli.md) `--debug`, acceptance checkbox in
  [10-acceptance.md](../spec/10-acceptance.md).

## 2026-07-17 — Streaming LLM spans (implemented)

- `ChatRequest.chatOnChunk` + engine `StreamDelta` (text / reasoning /
  complete tool call). Host ops stay atomic; partials are obs-only.
- `Hwfl.Obs.Stream` coalesces by char (64) and time (100ms) into
  `events.jsonl` (`message=llm.delta`) on the open span; `--debug` echoes
  coalesced lines (`llm Δ`).
- Mock emits fake 8-char chunks; llm-simple uses `streamTextWithFallbacks`
  when a chunk hook is set (object mode stays non-stream).
- Wired for `llm.chat` and `agent_round` model calls. Tests:
  `ProviderSpec` + `StreamSpec` (139 total).

## 2026-07-17 — Semantic-check deepen (same-run layer 3)

- Locked design: same-run optional pragmatic (not split
  `semantic-pragmatic` / JSON reload); proper body-bearing `review_gate`.
- Gate policy: similarity pairs → `check_redundancy` /
  `check_contradiction`; speech-act → `check_coverage_gap`; prose →
  `check_dead_reference`; unique-by `slice_id`, cap 8; entropy outliers
  stay layer-2 only.
- `mode=deterministic|pragmatic` + `model` inputs; pragmatic calls
  `llm.object` with `schema(PragmaticOut)` and maps to
  `pragmatic_findings` (felicity bleed filter).
- Engine fix: `loadTypeEnv` now runs `elaborateMainIO` before
  `inferModuleEnv` so modules with untyped `main(inputs)` keep type
  aliases for runtime `schema(T)` (was silently falling back to prelude).
- Deferred: CLI sugar, markdown summary, split packaging, skills wiring.
- 140 tests.

## 2026-07-17 — Semantic-check prose noise fix

- Workflow: scan only `workflows/` / `skills/` / `lib/` / `types/` (skip
  README docs); speech-act titles include `system` / `reviewer`; directive
  lexicon adds never / do not / don't.
- Pure prelude: `text.trim`, `text.starts_with`, `text.normalize_token`,
  `text.is_qname` (roots `workflows|lib|skills|tools|types|builtin`).
- Coding-agent dogfood: ~35 noisy findings → 2; `ok:true`.
- 143 tests.

## 2026-07-17 — Semantic-check research plan captured

- Wrote [semantic-check-plan.md](../semantic-check-plan.md): metaphor
  (“semantic type system” as checkable judgments, not FOL), shipped
  proto-judgments, strata (structure / normative / dynamic), phases
  S1–S6, deprioritize list, design locks. Suggested resume order: S2
  obligation graph first. Linked from TASKS Later / STATUS deferred.

## 2026-07-17 — Semantic-check A+B (contradiction + redundancy)

- **A (policy conflict):** gate `skills/*` and system/rules/constraints
  titles with `check_internal_conflict`; `PragmaticOut.contradictions`
  carry `quote_a` / `quote_b` / `why`; reviewer: only jointly
  unsatisfiable (conditionals OK). H1-only skills get a synthetic full-file
  slice (`{path}/skill`) because `md.sections` is H2/H3-only.
- **B (redundancy):** within-slice sentence pairs, similarity > 0.9,
  both sentences in evidence, cap 16; skip short bodies (`chars > 40`).
- Gate merge: unique by `(slice_id, review_task)`; policy before speech /
  prose / similarity; cap 8. Avoid reserved param name `task`.
- Fixture `skills/conflict-lang.md` + pragmatic mock asserts quoted
  GHC2021 vs Haskell2010. Coding-agent: deterministic `ok:true`, policy
  gates fire on skills.

## 2026-07-17 — `fs.patch` (Tier A)

- Host op `fs.patch({ path, hunks: List<{ old, new }> }) -> { ok, applied,
  error }`: each `old` must match exactly once after prior hunks; all-or-
  nothing write. Soft failure (`ok=false`) for not-found / ambiguous;
  empty `hunks` is a hard arg error.
- Kept `fs.edit` as intentional replace-all. Agent-tool `fs_patch`; coding-
  agent system prompt prefers patch for multi-site edits.
- Spec / language-reference updated. Tests: workspace helpers + machine
  run in `HostOpsSpec`.

## 2026-07-17 — Semantic-check S2 (obligation graph)

- Extended `PragmaticOut` with `obligations` extracted on every gated
  `llm.object` (no extra gate slots): actor / modality
  (`must|should|may|must_not`) / action / object / condition / quote.
- Deterministic graph on the accumulated set: must∧must_not (same key,
  conditional discharge preserved), workflows must vs skills may/should
  (info), catalog-missing qname objects; findings `category:obligation`;
  report also lists `obligations`.
- Fixtures `require-search` / `forbid-search` / `ghost-tool`; pragmatic
  mock asserts cross-module polarity + dead catalog. Next research phase:
  S1 role typing.

## 2026-07-17 — S2 crunch-budget fix

- Coding-agent pragmatic run hit `pure crunch limit exceeded` after
  `llm.object` (O(n²) obligation pair scans on chatty extracts).
- Fix: extract obligations only for `check_internal_conflict`; ≤4 per
  slice (scan ≤16); graph on ≤12 rows; skip soft pass when n>8;
  single-recursion pair walks; finding caps 4; raise pure crunch
  limit 100k→500k. Chatty-extract Spec regression.
## 2026-07-17 — Semantic-check S1 (illocutionary role typing)

- Extended `PragmaticOut` with forced `role`
  (`System`/`Policy`/`Procedure`/`Example`/`Rationale`/`ToolDoc`) and
  quoted `mismatched_sentences` on every gated `llm.object`.
- Deterministic felicity: Policy/System without directives; Example with
  unmarked hard constraints; findings `category:role`; report lists
  `roles`.
- Fixture `skills/example-hard-rule`; pragmatic mock asserts Example role
  + hard-constraint quote. Next research phase: S5 prose↔code contracts.

## 2026-07-17 — Semantic-check S5 (prose↔code contracts)

- Deterministic layer 2c in `examples/semantic-check`: dead bindable
  `@section`; prose host-op vs `effects`/`tools` (callable modules only);
  schema bullet fields vs typed `outputs:`; skill `exec.run` vs caller
  without `Exec` that names the skill; findings `category:contract` (cap 16).
- Fixtures `exec-gap` / `output-gap` / `skill-exec-gap` + `recommend-exec`;
  `ok` dead `@agent`. Coding-agent deterministic dogfood stays
  contract-clean. Next research: S3 / S4 / S6.

## 2026-07-17 — Semantic-check S3 (proposition algebra)

- Extended `PragmaticOut` with `propositions` (`must` / `must_not` /
  `prefer` / `prefer_not`, optional condition) on policy gates; also
  project from stamped obligations; merge/dedupe.
- Deterministic clashes: Must∧MustNot and unconditioned Must vs Prefer(~a);
  conditional discharge preserved; findings `category:proposition`; report
  lists `propositions`. Caps: ≤4/slice, ≤12 rows, finding caps 4.
- Fixture `prefer-no-search`; gate cap 8→10 so planted policy fixtures stay
  gated. Spec asserts Must∧MustNot + Prefer(~a). Next research: S4 / S6.

## 2026-07-17 — `hwfl run --cost`

- Host progress lines (`heLog`) optionally prefixed with running LLM
  spend (`$0.12 │ fs.read …`) via shared `ssRunCostMicros` / `runCostPrefix`
  (same counter as `--debug` span lines). Flag gates prefix only.

## 2026-07-17 — North star: library + workflow research lab

- **hwfl** = durable workflow runtime library (+ CLI). Longer aim: a
  **workflow research lab** (materialize candidates, run, score spans /
  outcome / cost, mutate) and a **separate** remote control plane
  (Postgres metadata, HTTP/WS pause-approve, tenants) — not Servant in
  this repo.
- Critical path reframed: library driver façade → run-store interface →
  `meta.invoke` / span introspection → local genetic prototype.
  Semantic-check parked (research / optional fitness). Coding-agent Tier
  A/B and RAG/MCP delayed until needed as a benchmark.
- Docs: [idea.md](../idea.md), [architecture.md](../architecture.md)
  (library vs control plane; project ≠ workspace), STATUS / TASKS,
  overview, semantic-check-plan status.

## 2026-07-17 — Library driver façade (P1)

- Added `Hwfl.Driver`: `driverCheck` / `driverRun` / `driverStep` /
  `driverResume` / `driverApprove` / `driverShow` — project-vs-module
  load+check+execute orchestration lives in the library, not the CLI.
- CLI (`app/Main.hs`) is flags + presentation + exit codes over that API.
- `DriverError` separates pre-run failures (parse/check/load) from
  `RunOutcome`; `jsonDriverError` for `--json`.
- Tests: `Hwfl.DriverSpec`. Next P1: run-store interface (FS backend).

## 2026-07-17 — Run-store interface (P1)

- Added `Hwfl.Runtime.Store`: opaque `RunStore`, `RunStoreBackend` record,
  FS backend preserving `.hwfl/runs/<id>/` layout (meta / snapshot /
  spans / events / transitions).
- Query API: `listRuns`, `readMeta`, `readSpans` (`SpanFilter`),
  `readSnapshot`, `readEventValues`; `createRun` / `openRun` (open does
  not mkdir empty dirs). Optional `StoreEvent` notify hook (noop in FS).
- Snapshot codecs stay in `Hwfl.Runtime.Snapshot`; file IO +
  `persistTransition` live in Store. Trace/Show no longer touch paths.
- Driver: `driverListRuns` / `driverReadMeta` / `driverReadSpans` /
  `driverReadSnapshot` / `driverOpenRun`.
- Tests: `Hwfl.Runtime.StoreSpec` (4 cases). Next: `meta.invoke` +
  `meta.list_runs` / `meta.read_spans`.

## 2026-07-18 — `meta.invoke` (nested lab runs)

- Host op `meta.invoke({ project, workspace, inputs? })` →
  `{ ok, run_id, status, outcome, error }` (recoverable; Meta+Read).
- Paths are **parent-workspace-relative** `FileRef`s; child durable state
  under the **child** workspace `.hwfl/runs/<run_id>/`.
- Implementation: shared `runTarget` in `Hwfl.Runtime.Run` (Driver wraps
  it); `HostEnv.heMetaInvoke` callback avoids Host↔Run import cycles.
- Infer special-cases `inputs` as any record (prelude stub uses Json).
- Missing module path returns `ok=false` / `status=error` (no IO throw).
- Same-project `FrInvoke` / E11 remains separate / deferred.
- Tests: `Hwfl.Runtime.MetaInvokeSpec` (2 cases). Next: `meta.list_runs` /
  `meta.read_spans`.

## 2026-07-18 — `meta.list_runs` / `meta.read_spans`

- Host ops for lab scoring over the run-store (Meta+Read, recoverable).
- `meta.list_runs({ workspace })` → `{ ok, runs, error }` (`runs` =
  `{ run_id, status, entry, started_at, project_hash }`).
- `meta.read_spans({ run_id, workspace, name_prefix?, kind?, limit? })` →
  `{ ok, spans, error }` (span records + Json `attrs`).
- Workspace paths are parent-sandbox-relative (`resolvePath`); missing run
  → `ok=false`. Implemented in `Host` (no Run callback).
- Infer special-cases optional span filters. Spec + language-reference
  updated.
- Tests: `Hwfl.Runtime.MetaReadSpec` (2 cases).

## 2026-07-18 — `meta.read_snapshot` (redacted)

- Host op `meta.read_snapshot({ run_id, workspace })` →
  `{ ok, snapshot: Json, error }` (Meta+Read, recoverable).
- Uses Store `openRun` + `readSnapshot`, then `redactJson . snapshotToJson`
  (same redaction path as CLI show — never raw cleartext secrets).
- Missing run / missing snapshot → `ok=false`, null snapshot, error string.
- Spec §05, language-reference, STATUS/TASKS updated; genetic prototype
  still next.
- Tests: `Hwfl.Runtime.MetaReadSpec` (3 cases: read + missing + sensitive
  key redaction).

## 2026-07-18 — Local compare / genetic prototype

- Shipped `examples/compare`: parent materializes `lean` / `rich` genomes
  into workspace `candidates/` + per-trial `trials/`, then
  `meta.check_project` → `meta.invoke` → `meta.read_spans` (all spans +
  `name_prefix = "llm."`). Ranks feasible trials by fewer LLM spans
  (cost proxy under mock); writes `results.json`.
- Genomes share `{ title, bullets, ok }` I/O; rich deliberately adds an
  extra `llm.chat` before `llm.object`. No new host ops.
- Tests: `Hwfl.Runtime.CompareSpec` (project check + mock run, winner =
  lean). Docs: STATUS / TASKS / spec §12 E22.

## 2026-07-18 — Observer hook (live span / pause)

- Added `Hwfl.Obs.Observer`: structured `ObsSpanOpen` / `ObsSpanClose` /
  `ObsPaused` / `ObsFinished` / `ObsProgress`; `noopObserver` /
  `stderrDebugObserver` / `mappendObserver`.
- Driver / Run take `Observer` (`drrObserver` / `roObserver`); CLI
  `--debug` installs stderr adapter (replaces hardcoded `ssDebug` Text
  logger). Step / resume / approve accept the same hook.
- Span open/close notify from Trace; pause/finish notify from
  `finalizeOutcome` and update `meta.json` status (`awaiting_confirm` /
  `paused` / `completed` / `failed`).
- Control-plane WS/SSE should map onto this callback — no FS scrape.
- Tests: `Hwfl.Obs.ObserverSpec` (3 cases).

## 2026-07-18 — Opt-in LLM transcripts (backlog)

- Spans stay the thin always-on index (topology, lengths, tokens, cost).
  Full LangSmith-style message in/out capture is **opt-in later**, as a
  separate channel joined by `span_id` (`transcripts.jsonl` / payloads) —
  not stuffed into span attrs, not default for every lab trial.
- Streaming `events.jsonl` remains live debug, not the durable archive.
- Spec sketch: [07-observability.md](../spec/07-observability.md) §10;
  backlog in TASKS Later / STATUS deferred.

## 2026-07-18 — Resume/approve project hash for project runs

- Bug: project starts pin `projectHashForModules`, but `loadExisting`
  recomputed `projectHashOf` (entry only) → always `stale project` on
  resume/approve for real projects (blocks hwfl-server confirm approve).
- Fix: walk parents of `rmEntry` for `project.json`; if found, rehash via
  `loadProject` / `projectHashForModules` and reload skills from that
  project root (workspace tip may differ). Lone modules unchanged.
- Spec §06 snapshots + E18 note; tests: project confirm→approve with
  separate project/workspace dirs, project stale refuse, HostOpsSpec
  exec-confirm starts via `driverRun`.
